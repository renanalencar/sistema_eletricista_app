export class Perfil {
	constructor(
		public nome: string, 
        public nota: number, 
        public email: string,
        public telefone: string
        //precisamos ver como fazer com info do cartao, mas temos que saber como usar pagar.me
	){}
}